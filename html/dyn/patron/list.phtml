<?
  include_once ("../../../lib/inc.php");

  if (file_exists (LIB. "/patrons.php"))
  {
    include_once (LIB. "/patrons.php");
    shuffle ($patrons);
    
    $apireturn["patrons"] = $patrons;
    $apireturn["status"]["rows"] = sizeof ($apireturn["patrons"]);
  }
  else 
  {
    $apireturn["status"]["rows"] = 0;
  }
  
  echo savecache ("/patron/list.json", apireturn ($apireturn));